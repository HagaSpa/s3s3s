name: release

on:
  push:
    branches:
      - master

jobs:
  release:
    
    runs-on: ubuntu-latest

    needs: build_test

    steps:
      - name: Create a Release
        uses: actions/create-release@v1
